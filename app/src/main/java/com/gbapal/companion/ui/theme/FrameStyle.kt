package com.gbapal.companion.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Neutral frame styling shared by the hub's banners/buttons/top bar: a plain
 * light-gray border plus a matching soft shadow tint. The only colour that
 * should stand out anywhere in this UI is Pokemon type colour (see
 * PokemonDetailScreen's TypeBadge/MoveCard) -- everything else stays
 * monochrome.
 */
val FrameOutline = Color(0xFFB0B0B0)
val FrameGlow = Color(0xFFD8D8D8)
